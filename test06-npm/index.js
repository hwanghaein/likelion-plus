console.log("hello npm");

// npm : 라이브러리, tool, 실행환경
// 라이브러리 
// 실행환경 도구 번역기 웹서버

// 배포할때는 라이브러리 필요
// 웹서버는 개발할때만 쓰는 도구 -> 배포할때 안가져감 (빌드할때 빠지게함)

// install 하면 해당 프로젝트에만 설치됨
// install -g 하면 프로젝트에 안들어가도 사용자 계정에 들어감. 두고두고 쓸 수있음

// --save-dev / -D 하면 배포할때 포함 안됨 
// dependencies VS devDependencies 

//  